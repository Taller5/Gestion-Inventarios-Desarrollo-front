import { createFileRoute } from '@tanstack/react-router'

export const Route = createFileRoute('/admin/$branchId/warehouses')({
  component: RouteComponent,
})

function RouteComponent() {
  return <div>Hello "/admin/$branchId/warehouses"!</div>
}
